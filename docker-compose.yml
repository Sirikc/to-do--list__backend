services:
  backend:
    build: ./
    env_file:
      - .env
    ports:
      - "5000:5000"
    depends_on:
      - db

  db:
    image: postgres:15
    ports:
      - "5432:5432"
    env_file:
      - .env
